import os
from src.constants import *

from dataclasses import dataclass

@dataclass
class DataIngestionConfig:
    DATA_INGESTION_ARTIFACTS_DIR = os.path.join(ARTIFACTS_DIR, DATA_INGESTION_ARTIFACTS_DIR)
    DATA_FILE_PATH = os.path.join(DATA_INGESTION_ARTIFACTS_DIR, DATA_FILE)


@dataclass
class DataTransformationConfig:
    DATA_TRANSFORMATION_ARTIFACTS_DIR = os.path.join(ARTIFACTS_DIR, DATA_TRANSFORMATION_ARTIFACTS_DIR)
    X_TRAIN_TRANSFORM_FILE_PATH = os.path.join(DATA_TRANSFORMATION_ARTIFACTS_DIR, X_TRAIN_TRANSFORM_FILE)
    X_TEST_TRANSFORM_FILE_PATH = os.path.join(DATA_TRANSFORMATION_ARTIFACTS_DIR, X_TEST_TRANSFORM_FILE)
    Y_TRAIN_TRANSFORM_FILE_PATH = os.path.join(DATA_TRANSFORMATION_ARTIFACTS_DIR, Y_TRAIN_TRANSFORM_FILE)
    Y_TEST_TRANSFORM_FILE_PATH = os.path.join(DATA_TRANSFORMATION_ARTIFACTS_DIR, Y_TEST_TRANSFORM_FILE)
    PREPROCESSOR_FILE_PATH = os.path.join(DATA_TRANSFORMATION_ARTIFACTS_DIR, PREPROCESSOR_FILE)

  
@dataclass
class ModelTrainerConfig:
    MODEL_TRAINER_ARTIFACTS_DIR = os.path.join(ARTIFACTS_DIR, MODEL_TRAINER_ARTIFACTS_DIR)
    MODEL_FILE_PATH = os.path.join(MODEL_TRAINER_ARTIFACTS_DIR, MODEL_FILE)
    METRICS_FILE_PATH = os.path.join(MODEL_TRAINER_ARTIFACTS_DIR, METRICS_FILE)
 

